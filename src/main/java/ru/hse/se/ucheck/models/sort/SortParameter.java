package ru.hse.se.ucheck.models.sort;

public enum SortParameter {
    PRICE, // sort in increasing order
    RATING, // sort in descending order
    OUTLET, // sort in alphabetical order
    DISTANCE // sort in increasing order
}
